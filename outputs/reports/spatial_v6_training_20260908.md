# v6正式训练运行记录

2026-09-08用户明确要求开始训练，此前生成后等待的限制已解除。10:31启动正式流水线，使用Anaconda `pubg`环境和RTX 5070 Ti；源码及配置冻结在本次输出目录，旧模型保留。

[W&B实时进度](https://wandb.ai/simonlynch-harbin-institute-of-technology/PUBGAudio/runs/235fa5787d18)，专用PUBGAudio项目中新运行，原声、合成音频、权重均不上传。

- 数据：已核验v6，训练20小时，验证/测试各2小时；每段120秒，训练输入5秒，输出间隔100ms。
- 预算：400轮，每轮7200个随机窗口、450次更新，总180000次更新；batch16、4个加载进程、4线程，Adam 0.001，无早停。
- 增强：50%空间镜像，随机裁剪；不额外添加噪声或增益增强。
- 主选模：验证集20°定位宏F1；另保留纯检测F1和总损失最佳模型。训练结束按三种预定规则各测试一次并导出，主模型为`model_localization.pt`，随后验证120秒CPU推理。

输出目录`outputs/train-spatial-v6-400epochs-20260908`。`pipeline_status.json`记录阶段；`metrics.jsonl`记录每轮结果；`pipeline.log`记录进度；`tracking-live/wandb_run.json`记录专用W&B身份。

2026-09-09提交前核验：400轮与预定测试/导出流水线已在2026-09-08 14:44（北京时间）完成，`pipeline_status.json`为`completed`，`export_verification.json`为`passed`。定位主模型选第371轮、检测模型第360轮、损失模型第367轮；主模型120秒CPU推理输出1200帧。权重仍仅本地保存。该运行使用原冻结代码，尚未拟合新增的置信概率校准，也未做实战准确率验证。

启动检查（10:35快照）：首2轮已完成900次优化，训练loss 1.1509→0.8009，验证loss 0.8008→0.5893，全部有限；last和三种best checkpoint已保存。W&B服务端状态running、epoch2及loss与本地一致。第二轮约40秒，据此初估全程4–5小时，实际随运行负载变化；早期数值不代表最终效果。

本次是已知客户端原声的新场景测试；与旧整枪型留出的56%不是同一测试协议，不能直接归因为单项改动提升。
